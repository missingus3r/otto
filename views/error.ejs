<%- include('partials/header', { pageTitle: 'Error ' + status }) %>

<main class="page">
  <div class="error-page">
    <h1><%= status %></h1>
    <p style="font-family:'Bodoni Moda',serif; font-style:italic; font-size:28px; margin:18px 0 32px; color: var(--paper-2);"><%= message %></p>
    <a class="btn" href="/"><%= t('common.back') %></a>
  </div>
</main>

<%- include('partials/footer') %>
